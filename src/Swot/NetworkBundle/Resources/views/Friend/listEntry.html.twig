<a class="hex small" href="{{ path('friend_show', {id: friend.id}) }}">
    <div class="hex-item">
        <div class="hex-item-inner">{{ friend.username }}</div>
    </div>
    <img class="maskborder opac" src="{{ (profile_image_folder ~ friend.profileImage|default('person.jpg')) | imagine_filter('profile_image_thumb_small') }}"/>
</a>


